name: "TODO"
on: ["push"]
jobs:
  build:
    runs-on: "ubuntu-latest"
    steps:
      - uses: "actions/checkout@main"
      - name: "todo-to-issue"
        uses: "senecajs/todo-to-issue-action@main"
        with:
          REPO: ${{ github.repository }}
          BEFORE: ${{ github.event.before }}
          SHA: ${{ github.sha }}
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LABEL: "TODO:"
          COMMENT_MARKER: "//"
          INCLUDE_EXT: ".ts,.js,.md"
        id: "todo"

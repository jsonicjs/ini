{
  "name": "@jsonic/ini",
  "version": "0.5.0",
  "description": "This plugin allows the [Jsonic](https://jsonic.senecajs.org) JSON parser to support INI syntax.",
  "main": "dist/ini.js",
  "type": "commonjs",
  "types": "dist/ini.d.ts",
  "homepage": "https://github.com/jsonicjs/ini",
  "keywords": [
    "json",
    "jsonic",
    "parser",
    "ini"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "repository": {
    "type": "git",
    "url": "git://github.com/jsonicjs/ini.git"
  },
  "scripts": {
    "test": "node --enable-source-maps --test \"dist-test/*.test.js\"",
    "test-some": "node --enable-source-maps --test-name-pattern=\"$npm_config_pattern\" --test \"dist-test/*.test.js\"",
    "watch": "tsc --build src test -w",
    "build": "tsc -p node_modules/@jsonic/hoover/src/tsconfig.json && tsc --build src test",
    "clean": "rm -rf dist dist-test node_modules yarn.lock package-lock.json",
    "doc": "jsonic-doc",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run doc && npm run repo-tag && npm publish --access public --registry https://registry.npmjs.org "
  },
  "license": "MIT",
  "files": [
    "src",
    "dist",
    "LICENSE"
  ],
  "devDependencies": {
    "@hapi/code": "^9.0.3",
    "@jsonic/doc": "^0.0.9",
    "@jsonic/hoover": "github:jsonicjs/hoover#main",
    "@types/node": "^25.4.0",
    "jsonic": "github:jsonicjs/jsonic#main",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "@jsonic/hoover": ">=0.9.0",
    "jsonic": ">=2.21.1"
  }
}
